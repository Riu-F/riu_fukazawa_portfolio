Affinity diagram photos

Add, remove, or replace image files here (.png, .jpg, .jpeg, .svg, .webp).
Files are listed alphabetically by name (use affinity-01.png, affinity-02.png, … for order).

Non-image files (like this README) are ignored by the site.

No code changes needed — the Insights accordion gallery reads this folder automatically.
After deploy, run a new build if images changed on the server.
